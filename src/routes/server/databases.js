export default function render(params) {
  return `
    <div class="page databases-page">
      <h1>Databases</h1>
      <p>Server ${params.id} databases management coming soon.</p>
    </div>
  `;
}
